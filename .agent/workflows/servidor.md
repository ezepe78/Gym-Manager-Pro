---
description: Enciende el servidor de desarrollo y abre la previsualización en el navegador
---

Para arrancar el entorno de desarrollo y ver los cambios:

// turbo
1. Ejecuta el comando `npm run dev` en la terminal.
2. Una vez que el servidor esté listo, abre la aplicación en el navegador visitando `http://localhost:5000/`.
